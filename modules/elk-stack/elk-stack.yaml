# Default values for elk.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
elasticsearch:
  enabled: false

kibana:
  enabled: true
  env:
    ELASTICSEARCH_HOSTS: http://elk-stack-elasticsearch-client:9200

logstash:
  enabled: true
  elasticsearch:
    host: elk-stack-elasticsearch-client

filebeat:
  enabled: true
  config:
    output.file.enabled: false
    output.logstash:
      hosts: ["elk-stack-logstash:5044"]
  indexTemplateLoad:
    - elk-stack-elasticsearch-client:9200

fluentd:
  enabled: false

fluent-bit:
  enabled: false

fluentd-elasticsearch:
  enabled: false

nginx-ldapauth-proxy:
  enabled: false
  # Example config to get it working with ELK. Adjust as you need to.
  # proxy:
  #   port: 5601
  #   # This is the internal hostname for the kibana service
  #   host: "elk-kibana.default.svc.cluster.local"
  #   authName: "ELK:Infrastructure:LDAP"
  #   ldapHost: "ldap.example.com"
  #   ldapDN: "dc=example,dc=com"
  #   ldapFilter: "objectClass=organizationalPerson"
  #   ldapBindDN: "cn=reader,dc=example,dc=com"
  #   requires:
  #     - name: "ELK-USER"
  #       filter: "cn=elkuser,ou=groups,dc=example,dc=com"
  # ingress:
  #   enabled: true
  #   hosts:
  #     - "elk.example.com"
  #   annotations:
  #     kubernetes.io/ingress.class: nginx
  #   tls:
  #     - hosts:
  #       - elk.example.com
  #       secretName: example-elk-tls
  # secrets:
  #   ldapBindPassword: PASSWORD
elasticsearch-curator:
  enabled: false

elasticsearch-exporter:
  enabled: false

dependencies:
- name: elasticsearch
  version: ^1.32.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: elasticsearch.enabled
- name: kibana
  version: ^7.7.1
  repository: https://helm.elastic.co
  condition: kibana.enabled
- name: filebeat
  version: ^^7.7.1
  repository: https://helm.elastic.co
  condition: filebeat.enabled
- name: logstash
  version: ^^7.7.1
  repository: https://helm.elastic.co
  condition: logstash.enabled
- name: fluentd
  version: ^2.2.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: fluentd.enabled
- name: fluent-bit
  version: ^2.8.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: fluent-bit.enabled
- name: fluentd-elasticsearch
  version: ^2.0.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: fluentd-elasticsearch.enabled
- name: nginx-ldapauth-proxy
  version: ^0.1.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: nginx-ldapauth-proxy.enabled
- name: elasticsearch-curator
  version: ^2.1.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: elasticsearch-curator.enabled
- name: elasticsearch-exporter
  version: ^2.1.0
  repository: https://kubernetes-charts.storage.googleapis.com/
  condition: elasticsearch-exporter.enabled